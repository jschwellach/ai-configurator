name: "syntax-error"
description: "Profile with YAML syntax errors"
contexts:
  - "context1.md"
  - "context2.md"
    invalid_indentation: true
hooks:
  on_session_start:
    - name: "test"
      enabled: true
    - name: "broken"
      enabled: false
      invalid_yaml: [unclosed_list